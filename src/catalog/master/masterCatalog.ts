import { CatalogProduct } from '../types/productTypes';

// Master catalog can be hydrated from an external API (like Open Food Facts)
// or a static JSON list of common FMCG products.
export const masterCatalog: CatalogProduct[] = [];
